function mostrar()
{
    var clave;
    clave = prompt("¿Cuál es la clave?");
    while(clave != "utn750")
    {
        clave = prompt("Contraseña incorrecta");
    }

    /*do {

        if (clave == undefined)
        {
            clave = prompt("Ingrese contraseña:");
        } else {
            clave = prompt("Incorrecto, ingrese contraseña:");
        }

    } while (clave != "utn750");*/
    

}//FIN DE LA FUNCIÓN
